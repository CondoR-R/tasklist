function Footer() {
  return (
    <footer className="footer">
      <p>
        Технологии и используемые концепции React: React, JSX, props, useState,
        композиция компонентов, условный рендеринг, методы массива (map,
        фильтр), обработка событий.
      </p>
    </footer>
  );
}

export default Footer;
